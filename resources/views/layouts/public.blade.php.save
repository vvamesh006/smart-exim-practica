<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@yield('title', 'Smart-Exim') — Smart-Exim SRL</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 flex flex-col min-h-screen">

    <header class="bg-white shadow-sm sticky top-0 z-50">
        <nav class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between">
            <a href="{{ route('home') }}">
                <img src="{{ asset('images/logo.png') }}" alt="Smart-Exim" class="h-20 w-auto">
            </a>
            <div class="hidden md:flex gap-6 items-center">
                <a href="{{ route('home') }}" class="hover:text-green-700">Acasă</a>
                <a href="{{ route('products') }}" class="hover:text-green-700">Produse</a>
                <a href="{{ route('services') }}" class="hover:text-green-700">Servicii</a>
                <a href="{{ route('about') }}" class="hover:text-green-700">Despre noi</a>
                <a href="{{ route('contact') }}" class="hover:text-green-700">Contact</a>
                @auth
                    <a href="{{ route('admin.dashboard') }}" class="text-green-700 font-semibold hover:underline">Panou admin</a>
                    <form method="POST" action="{{ route('logout') }}" class="inline">
                        @csrf
                        <button type="submit" class="text-gray-500 hover:text-red-600">Ieșire</button>
                    </form>
                @else
                    <a href="{{ route('login') }}" class="bg-green-700 text-white px-4 py-2 rounded-lg hover:bg-green-800">Autentificare</a>
                @endauth
            </div>
        </nav>
    </header>

    <main class="flex-grow">
        @yield('content')
    </main>

    <footer class="bg-gray-800 text-gray-300 mt-12">
        <div class="max-w-6xl mx-auto px-4 py-8 flex flex-col md:flex-row justify-between gap-4">
            <div>
                <img src="{{ asset('images/logo.png') }}" alt="Smart-Exim" class="h-12 w-auto bg-white rounded-lg p-1 mb-2">
                <p class="text-sm mt-1">Comerț cu ridicata și transport de mărfuri</p>
            </div>
            <div class="text-sm">
                <p>Email: contact@smart-exim.md</p>
                <p>Tel: +373 22 000 000</p>
            </div>
        </div>
        <div class="bg-gray-900 text-center text-xs py-3">
            © {{ date('Y') }} Smart-Exim SRL. Toate drepturile rezervate.
        </div>
    </footer>

</body>
</html>
